diff --git a/ElementsKernel/scripts/squeezedInstall_test b/ElementsKernel/scripts/squeezedInstall_test
index e7a9450d..64fb1ea1 100755
--- a/ElementsKernel/scripts/squeezedInstall_test
+++ b/ElementsKernel/scripts/squeezedInstall_test
@@ -16,7 +16,7 @@ tmploc=$(mktemp -dq -t temp.XXXXXX)
 custom_prefix=${tmploc}/usr/local
 mkdir -p ${custom_prefix}
 
-cmake_full_cmd="cmake -DCMAKE_PREFIX_PATH=${custom_prefix} -DCMAKE_INSTALL_PREFIX=${custom_prefix}"
+cmake_full_cmd="cmake -DCMAKE_PREFIX_PATH=${custom_prefix} -DCMAKE_INSTALL_PREFIX=${custom_prefix} -DUSE_SPHINX=OFF"
 
 
 # prepare the environment
@@ -101,7 +101,7 @@ mkdir -p ${User_area}
 
 cd ${User_area}
 
-CreateElementsProject -n --yes ${projectB_name} ${projectB_version} 
+CreateElementsProject -n --yes ${projectB_name} ${projectB_version}
 if [ $? -ne 0 ]; then
    echo "Error: <CreateElementsProject $projectB_name $projectB_version> command failed!" 1>&2
    local_clean_exit 1
